# ── Core web framework ───────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
email-validator>=2.0.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9

# ── Auth / security ───────────────────────────────────────────────────────────
pyjwt>=2.9.0
bcrypt>=4.2.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0

# ── Image / numeric base ─────────────────────────────────────────────────────
pillow>=10.4.0
numpy>=1.26.0

# ── OCR dependencies ─────────────────────────────────────────────────────────
# PDF native text extraction
pypdf==4.3.1

# OpenCV for image preprocessing (deskew, denoise, binarise)
opencv-python-headless==4.10.0.84

# Tesseract Python bindings (requires Tesseract binary + hin/kan/eng packs)
# Install Tesseract: https://github.com/UB-Mannheim/tesseract/wiki
pytesseract==0.3.13

# PaddleOCR multilingual OCR (English PP-OCRv6, Hindi PP-OCRv5, Kannada PP-OCRv3)
# Requires paddlepaddle CPU wheel (auto-installed as dependency)
paddleocr==3.7.0
paddlepaddle==3.3.1

# ── Local neural translation (Kannada/Hindi/Marathi → English) ──────────────
# The first translation downloads public OPUS-MT checkpoints into the local
# Hugging Face cache; no API credential is required.
torch>=2.6.0
transformers>=4.51.0,<5.0.0
sentencepiece>=0.2.0
sacremoses>=0.1.1
